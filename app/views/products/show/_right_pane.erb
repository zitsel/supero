<h1><%=@product.brand.try(:titleize)%></h1>
<p><%=@product.description.try(:titleize)%></p>
<p>Size: <%=@product.size.try(:titleize)%></p>
<p><%=number_to_currency(@product.price)%></p>
<p>Condition: <%=@product.condition_description%></p>

<div class="panel-group" id="accordion">
  <div class="panel panel-default">
    <div class="panel-heading">
      <h4 class="panel-title">
        <a data-toggle="collapse" data-parent="#accordion" href="#collapseOne">
          Product Details
        </a>
      </h4>
    </div>
    <div id="collapseOne" class="panel-collapse collapse in">
      <div class="panel-body">
        <table class="table table-striped">
          <tbody>
            <% @product.details.each do |k,v| %>
              <% unless v=="" %>
                <tr><td><%=k.try(:titleize)%>:</td><td><%=v.try(:titleize)%></td></tr>
              <%end%>
            <%end%>
          </tbody>
        </table>
      </div>
    </div>
  </div>
  <div class="panel panel-default">
    <div class="panel-heading">
      <h4 class="panel-title">
        <a data-toggle="collapse" data-parent="#accordion" href="#collapseTwo">
          Product Measurements
        </a>
      </h4>
    </div>
    <div id="collapseTwo" class="panel-collapse collapse">
      <div class="panel-body">
              <table class="table table-striped">
          <tbody>
            <% @product.measurements.each do |k,v| %>
              <% unless v=="" %>
                <tr><td><%=k.try(:titleize)%>:</td><td><%=v.try(:titleize)%></td></tr>
              <%end%>
            <%end%>
          </tbody>
        </table>
      </div>
    </div>
  </div>
  <div class="panel panel-default">
    <div class="panel-heading">
      <h4 class="panel-title">
        <a data-toggle="collapse" data-parent="#accordion" href="#collapseThree">
          Free Alterations
        </a>
      </h4>
    </div>
    <div id="collapseThree" class="panel-collapse collapse">
      <div class="panel-body">
          Basic alterations are <i>free</i> with the purchase of this item.
      </div>
    </div>
  </div>
  <div class="panel panel-default">
    <div class="panel-heading">
    <h4 class="panel-title">
      <a data-toggle="collapse" data-parent="#accordion" href="#collapseFour">
       Free Delivery &amp; Returns 
      </a>
    </h4>
    </div>
    <div id="collapseFour" class="panel-collapse collapse">
      <div class="panel-body">
        Ground shipping is free anywhere in the United States. <br />
        Your satisfaction is guaranteed. You may return your item for <i>any reason</i> within 30 days of your order.
      </div>
    </div>
  </div>
</div>
  <button type="button" class="btn btn-default pull-right" data-toggle="popover" data-placement="right" data-content="Sorry! Purchasing isn't available through our website yet. Call us at 515.619.9037 or e-mail sales@revive-clothiers.com!">Add to Cart</button>


