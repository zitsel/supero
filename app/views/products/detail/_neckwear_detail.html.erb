<%= f.input :brand, collection: @product.brands%>
<%= f.input :label%>
<%= f.input :retailer%>
<%= f.input :material, collection: @product.materials%>
<%= f.input :width, collection: @product.widths%>
<%= f.input :length, collection: @product.lengths%>
<%= f.input :color %>
<%= f.input :pattern %>
<%= f.input :weave, collection: @product.weaves%>
<%= f.input :style, collection: @product.styles%>
