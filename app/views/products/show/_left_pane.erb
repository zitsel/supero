<div class="main_img crop">
	    <span class='zoom' id='zoom_main_img'>
	    <%=image_tag(@product.main_photo, id: "img_to_change")%>

	</span>
	<br />
		</div>
			    <%=link_to "supersize image", image_path(@product.main_photo), :id=>"link_to_change", :class=>"pull-right", :data=>{:colorbox=>true}%>

		<div class="thumbs">
		    <%@product.ordered_photos.each do |photo|%>
			   
			    <%= image_tag(photo.uploaded_file(:thumb), :class=>"thumb")%>
			
		    <%end%>

			    </div><!--row thumbs-->
			    
			<script>
			    $(document).ready(function(){
				$('#zoom_main_img').zoom();
				});
			</script>
			<script>
			    $(function() {
				$('.thumb').click(function(e){
				     e.preventDefault();
				     var $new = $( this ).attr("src").replace('thumb','original')
				    $("#img_to_change").attr('src',$new);
				    $("#link_to_change").attr('href',$new);
				    $('#zoom_main_img').zoom();
				 });
			    });
			</script>
